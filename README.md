# 🎮 Gobblet Gobblers Tic Tac Toe

A modern, futuristic neon-styled web game inspired by Marvel Snap. Players can play against an AI bot or against a friend in a strategic stacking tic-tac-toe variant.

---

## 📁 Project Structure

### **1. งาน.html** (Main Menu Page)
**ไฟล์หลักสำหรับหน้าเมนูเริ่มต้น**

**ทำหน้าที่:**
- แสดงหน้าเลือกโหมดเกม
- ให้ผู้เล่นเลือกระหว่าง:
  - 🤖 **เล่นกับบอท** → ไปหน้าเลือกระดับความยาก
  - 👥 **เล่น 2 คน** → เริ่มเกมทันที

**เนื้อหา:**
- Logo และชื่อเกม
- ปุ่มเลือกโหมด 2 ปุ่ม
- Footer ลิขสิทธิ์

---

### **2. menu.css**
**ไฟล์ CSS สำหรับตกแต่งหน้าเมนู**

**ประกอบไปด้วย:**

#### **a) Variables (ตัวแปรสีและการออกแบบ)**
```css
--bg-0: #06080b        /* สีพื้นหลังเข้ม */
--bg-1: #0f1220        /* สีเรดเดียนต่อ */
--accent: #7afcff      /* สีไฮไลต์ฟ้า (Cyan) */
--accent-2: #ff6b6b    /* สีไฮไลต์แดง */
--muted: #98a1c3       /* สีข้อความอ่อน */
```

#### **b) Background Effects**
- **Animated Orbs** – วงกลมสีฟ้าและแดงที่ลอยไปมา
- **Grid Pattern** – เส้นตาราง subtle แบบ cyber
- **Gradient Background** – พื้นหลังทรีโทนเข้มลึก

#### **c) Animations**
- `float` – ทำให้วงกลม Orb ลอยไปมา
- `glow-pulse` – ทำให้ชื่อเกมหลุดมองวาวไฟ
- `ripple` – เอฟเฟกต์ระลอกน้ำเมื่อกด
- `icon-bounce` – Emoji ของปุ่มบ้อนตัวเพลง
- `press` – เอฟเฟกต์สเงาเมื่อกดปุ่ม

#### **d) Button Styles**
- `p1` – ปุ่มเล่นกับบอท (เขียว)
- `p2` – ปุ่มเล่น 2 คน (แดง)
- Hover effects, Active effects, Smooth transitions

---

### **3. difficulty.html**
**หน้าเลือกระดับความยาก**

**ทำหน้าที่:**
- แสดงตัวเลือกระดับบอท 4 ระดับ:
  1. **ง่าย** 🟢 – บอทเลือกแบบสุ่ม
  2. **ปานกลาง** 🟠 – บอทหาจุดชนะและป้องกัน
  3. **ยาก** 🔴 – บอทรวมทั้งสอง + ตรรมชาติเพิ่มเติม
  4. **Super Hard 🧠** 🟣 – Minimax AI ที่คิดลึกลำดับไป

**เนื้อหา:**
- ปุ่มเลือกความยาก 4 ปุ่ม
- ปุ่มย้อนกลับ
- ส่งพารามิเตอร์ `mode=1p&difficulty={level}&player=P1` ไปยัง `index.html`

---

### **4. index.html**
**หน้าเล่นเกม**

**ทำหน้าที่:**
- แสดงกระดานเกม 3x3
- แสดงแถบข้าง (Sidebar) ด้านขวา
- ลิงก์ JavaScript ไปยัง `script.js`

**องค์ประกอบ:**
- `<div id="board">` – กระดานเกม 9 ช่อง
- `<div class="sidebar">` – แถบข้างแสดง:
  - ผู้เล่นปัจจุบัน
  - ปุ่มเลือกขนาดหมาก (เล็ก/กลาง/ใหญ่)
  - จำนวนหมากเหลือแต่ละขนาด
  - ปุ่มเริ่มใหม่
  - ปุ่มกลับหน้าหลัก
- `<div id="win-overlay">` – ป๊อปอัปแสดงผลแพ้/ชนะ

---

### **5. style.css**
**ไฟล์ CSS สำหรับตกแต่งหน้าเล่น**

**ประกอบไปด้วย:**

#### **a) Color Variables**
- พื้นหลัง, Accent, Glass effects, Shadows

#### **b) Layouts**
- **Body** – Flexbox centered layout
- **Game Container** – Board และ Sidebar แบบ flex
- **Board** – Grid 3x3
- **Sidebar** – Vertical stack ของ UI elements

#### **c) Cell & Piece Styles**
- `.cell` – ช่องเล่นแต่ละช่อง
- `.piece` – หมากที่วางบนกระดาน
- **X pieces** (แดง) – small/medium/large
- **O pieces** (น้ำเงิน) – small/medium/large

#### **d) Buttons**
- `#reset` – ปุ่มเริ่มใหม่ (สีแดง)
- `#home` – ปุ่มกลับหน้าหลัก (สีเขียว)
- Piece selector buttons

#### **e) Win Overlay**
- `#win-overlay` – Backdrop สีดำ
- `.win-modal` – Box แสดงผล
- `.win-actions` – ปุ่ม Restart/Home

---

### **6. script.js**
**ไฟล์ JavaScript ที่เก็บตรรมชาติการเล่นเกมทั้งหมด**

**โมดูล/ส่วนประกอบ:**

#### **a) Game State Variables**
```javascript
mode        // โหมด: "1p" (เล่นกับบอท) หรือ "2p" (2 คน)
difficulty  // ระดับ: "easy", "medium", "hard", "super"
currentPlayer // ผู้เล่นปัจจุบัน: "X" หรือ "O"
board       // Array 9 ช่อง, แต่ละช่องเป็น stack ของหมาก
piecesLeft  // จำนวนหมากเหลือแต่ละผู้เล่น
```

#### **b) Core Functions**

| ฟังก์ชัน | ใช้ทำอะไร |
|---------|---------|
| `createBoard()` | สร้างกระดาน 3x3 HTML elements |
| `handleCellClick(index)` | จัดการการคลิกบนช่องเล่น |
| `canPlace(index, player, size, isMove)` | ตรวจสอบว่าลงหมากได้ไหม |
| `renderBoard()` | วาดหมากใหม่บนกระดาน |
| `switchTurn()` | สลับตาผู้เล่น |
| `checkWinner()` | ตรวจสอบว่ามีผู้ชนะไหม |
| `endGame(msg)` | จบเกมแสดงผล |

#### **c) Bot AI Functions**

| ฟังก์ชัน | ใช้ทำอะไร | ระดับ |
|---------|---------|------|
| `botMove()` | ตัดสินใจท่าบอท | ทั้งหมด |
| `generateAllMoves(player)` | สร้างรายการท่าที่ถูกต้องทั้งหมด | ทั้งหมด |
| `findWinningMoveGeneric(player)` | หาท่าชนะทันที | medium/hard/super |
| `findBlockingMoveGeneric(bot)` | หาท่าป้องกันผู้เล่น | medium/hard/super |
| `minimaxBestMove(bot)` | Minimax algorithm ลึก 6 ขั้น | super |
| `minimax(depth, isMax, ...)` | Recursion Minimax with alpha-beta | super |

#### **d) Difficulty Logic**
- **Easy** – สุ่มเลือกท่า
- **Medium** – หาชนะหรือป้องกัน แล้วสุ่ม
- **Hard** – เดียวกับ medium
- **Super Hard** – Minimax ที่คิดลึก 6 ระดับด้วย alpha-beta pruning

#### **e) UI Update Functions**
- `updatePieceCounts()` – อัปเดตจำนวนหมากเหลือ
- `updateGameInfo()` – แสดงโหมด/ระดับบน UI
- `showWinPopup(text)` – แสดง Popup ชนะ พร้อมปุ่ม Restart/Home

#### **f) Query Parameter Reading**
```javascript
const params = new URLSearchParams(window.location.search);
mode = params.get('mode');           // จากเมนู
difficulty = params.get('difficulty'); // จากหน้า difficulty
currentPlayer = params.get('player'); // เลือก P1 (X) หรือ P2 (O)
```

---

## 🎮 Game Logic Explanation

### **Game Rules (Gobblet Gobblers)**

1. **กระดาน** – 3×3 grid
2. **หมาก** – 2 ชุด (X และ O) แต่ละชุด 6 หมาก:
   - 2 หมากเล็ก
   - 2 หมากกลาง
   - 2 หมากใหญ่

3. **วิธีเล่น**:
   - วางหมากลงบนช่องว่างหรือ stack บนหมากเก่า
   - **ต้องวางหมากใหญ่กว่า** เท่านั้น (เช่น medium ทับ small ได้)
   - ลำดับการเลือก:
     - a) เลือกขนาดหมาก → คลิกช่องเพื่อวาง
     - b) หรือ คลิกหมากของคุณ → คลิกช่องปลายทาง

4. **ชนะ** – 3 ตัวติดกัน (แนวนอน/แนวตั้ง/แนวทแยง) ของสีเดียว

### **Bot AI Strategy**

**Easy** 🟢
- สุ่มท่าใดก็ได้ที่ถูกต้อง

**Medium** 🟠
- หาท่าชนะทันที → วาง
- ถ้าไม่มี หาท่าป้องกัน → วาง
- ถ้าไม่มี สุ่มเลือก

**Hard** 🔴
- เดียวกับ Medium

**Super Hard 🧠**
- **Minimax Algorithm** ที่คิดหลาย ๆ ท่าข้างหน้า
- **Alpha-Beta Pruning** เพื่อลดการคำนวณ
- **Depth Limit 6** – คิดลึก 6 ขั้นเพื่อไม่ให้ช้าเกินไป

---

## 📊 File Dependencies

```
งาน.html (Menu)
    ↓ onclick="chooseMode('1p')"
    ↓ 
difficulty.html
    ↓ onclick="choose('easy'|'medium'|'hard'|'super')"
    ↓
index.html (Game)
    ├→ script.js (Game Logic)
    └→ style.css (Game Styling)

งาน.html
    └→ menu.css (Menu Styling)
```

---

## 🎨 Design Highlights

- **Neon/Cyberpunk UI** – Dark background with glowing cyan & red accents
- **Glass Morphism** – Frosted glass effect on panels
- **Smooth Animations** – Hover, click, and transition effects
- **Responsive Design** – Works on desktop and mobile (with viewport meta)
- **Arcade Feel** – Orbitron font for titles, Inter for text

---

## 🔧 How to Use

1. **Open `งาน.html`** in a web browser
2. **Select Game Mode**:
   - 🤖 Bot → Choose difficulty → Play
   - 👥 2 Players → Play immediately
3. **Game Controls**:
   - Select piece size → Click cell to place
   - OR Click your piece → Click destination to move
4. **Win Screen** → Restart or Return to Menu

---

## 📝 Technical Notes

- **Vanilla JavaScript** – No frameworks (pure HTML/CSS/JS)
- **No Build Tools** – Drop in any folder and open HTML
- **Browser Compatible** – Modern browsers (Chrome, Firefox, Safari, Edge)
- **Responsive** – Mobile-friendly viewport settings
- **Accessible** – Proper semantic HTML and ARIA attributes

---

**© 2025 XO Arcade** 🎮
